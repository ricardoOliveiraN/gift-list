# Desenvolvimento (npm run dev)
# URL consumida no front. Mantemos /api para usar o proxy do Vite.
VITE_API_URL=/api

# Backend real de desenvolvimento para onde o Vite vai redirecionar /api.
# Troque para o IP/porta do seu backend dev.
VITE_DEV_API_TARGET=http://localhost:8080
