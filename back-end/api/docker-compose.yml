services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gifts-api
    environment:
      SERVER_PORT: ${APP_PORT}
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${APP_PORT}:8080"
